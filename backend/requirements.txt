# Candor — Backend dependencies
# Install: pip install -r requirements.txt

# Web framework
fastapi>=0.111.0,<1.0.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9       # required for File/UploadFile

# Settings & validation
pydantic-settings>=2.3.0
python-dotenv>=1.0.1

# Database
supabase>=2.5.0

# PDF parsing (text-layer primary path)
pdfplumber>=0.11.0

# OCR fallback path
pytesseract>=0.3.13
pdf2image>=1.17.0
Pillow>=10.4.0

# LLM — Gemini function-calling, temperature 0 (new official SDK)
google-genai>=1.0.0

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.0
httpx>=0.27.0                 # async test client for FastAPI
